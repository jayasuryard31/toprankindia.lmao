# Leave unset to run against the in-browser demo backend (src/services/mock).
# Set it, and every service switches to real HTTP calls — no UI changes needed.
# VITE_API_BASE_URL=http://localhost:5000/api

# Force the demo backend even when a base URL is configured.
# VITE_USE_MOCK_API=true
